import { StyleSheet, Text, View } from "react-native";
import React from "react";

const RequestSentScreen = () => {
  return (
    <View>
      <Text>RequestSentScreen</Text>
    </View>
  );
};

export default RequestSentScreen;

const styles = StyleSheet.create({});
